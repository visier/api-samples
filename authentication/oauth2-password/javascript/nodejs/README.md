# JavaScript Password Grant Authentication
## Usage
1. Update the environment file with the appropriate values.
1. Source the environment file: `source .env`
1. Call the sample application: `node index.js`
